{% extends "base.html.twig" %}

{% block title %}{{parent()}}Tâche{% endblock %}

{% block contenu %}

<div class="container-fluid">           
       
    <div class="card mx-auto mb-3" style="max-width: 840px;">
        <div class="row no-gutters">
            <div class="col-md-8">
                <div class="card-body">
                    <h5 class="card-title text-center">{{tache.libelle}}</h5>
                    <p class="card-text">Temps de la tache {{tache.temps_tache}}</small></p> 
                    <p class="card-text">Status : {{tache.status}}</small></p> 
                    <p class="card-text">Coût : {{tache.cout}}€</small></p> 
                    <p class="card-text">Date de début : {{tache.dateDebut|date('d-m-Y', false)}}</small></p> 
                    <p class="card-text">Date de fin : {{tache.dateFin|date('d-m-Y', false)}}</small></p> 

                     
                    {#<p class="card-text">Adresse :</small></p> 
                    
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">Ville : <small class="text-muted">{{client.adr_ville}}</small></li>
                        <li class="list-group-item">Code Postal : <small class="text-muted">{{client.adr_cp}}</small></li>
                        <li class="list-group-item">Rue : <small class="text-muted">{{client.adr_rue}}</small></li>
                        <li class="list-group-item">Numéro : <small class="text-muted">{{client.adr_no}}</small></li>
                    </ul> #}
                </div>
            </div>
        </div>
    </div>


    {#<div class="row justify-content-center">            
        <h1 class="text-center text-primary p-4">Liste des projets</h1>        
    </div>     

    <div class="row justify-content-center">            
        <div class="col-8 bg-white p-4 m-0 text-dark"> 
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">Nom du projet</th>
                        <th scope="col">Date de signature du contrat</th>
                        <th scope="col"></th>
                        <th scope="col"></th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    {% for projet in liste %}
                        <tr class="{{ cycle(['table-secondary', 'table-light'], loop.index0)}}">
                            <td>{{projet.projetlib}}</td>
                            <td>{{projet.sign}}</td>
                            <td><a href="index.php?page=projet&id={{projet.id_proj}}"<span class="material-icons text-dark" title="Voir le projet">find_in_page</span></a></td>
                            <td><a href="index.php?page=listeProjets&id={{projet.id_proj}}"<span class="material-icons text-dark" title="Modifier le projet'">create</span></a></td>
                            <td><a href="index.php?page=listeProjets&supp={{projet.id_proj}}"<span class="material-icons text-dark" title="Supprimer le projet">delete</span></a></td>
                        </tr>
                    {% endfor %}
                </tbody>    
            </table>                
        </div>        
    </div>#}
</div>

<!--{% set nb = form.nbpages %}
    <nav aria-label="Page navigation">
        <ul class="pagination justify-content-center m-4 p-4">
            <li class="page-item-alert {% if form.nopage == 0 %}disabled{% endif %}">
                <a class="page-link" href="index.php?page=liste_projets&nopage={{ 0 }}" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                </a>
            </li>
            {% for i in 1..nb %}
                <li class="page-item-alert {% if i ==form.nopage +1 %} active{% endif %}">
                    <a class="page-link" href="index.php?page=liste_projets&nopage={{ i-1 }}">
                    {{ i }}
                    <span class="sronly"></span>
                    </a>
                </li>
            {% endfor %}
            <li class="page-item-alert {% if form.nopage == form.nbpages-1 %}disabled{% endif %}">
                <a class="page-link" href="index.php?page=liste_projets&nopage={{ nb-1 }}" aria-label="Next" >
                    <span aria-hidden="true">&raquo;</span>
                </a>
            </li>
        </ul>
    </nav>
    -->
{% endblock %}